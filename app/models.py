"""
Database Models for AI Browser Automation
"""
import json
from datetime import datetime
from flask_sqlalchemy import SQLAlchemy
from sqlalchemy.orm import DeclarativeBase
from app.utils.encryption import credential_encryption


class Base(DeclarativeBase):
    pass


db = SQLAlchemy(model_class=Base)


class AutomationPlan(db.Model):
    """Model for storing automation plans generated by Planner Agent"""
    
    __tablename__ = 'automation_plans'
    
    id = db.Column(db.Integer, primary_key=True)
    created_at = db.Column(db.DateTime, nullable=False, default=datetime.utcnow, index=True)
    execution_id = db.Column(db.Integer, db.ForeignKey('execution_history.id'), nullable=True)
    
    goal = db.Column(db.Text, nullable=False)
    start_url = db.Column(db.Text, nullable=False)
    plan_yaml = db.Column(db.Text, nullable=False)
    validated_locators = db.Column(db.Text, nullable=True)
    plan_metadata = db.Column(db.Text, nullable=True)
    
    def to_dict(self):
        """Convert model to dictionary"""
        return {
            'id': self.id,
            'created_at': self.created_at.isoformat() if self.created_at else None,
            'execution_id': self.execution_id,
            'goal': self.goal,
            'start_url': self.start_url,
            'plan_yaml': self.plan_yaml,
            'validated_locators': self.validated_locators,
            'plan_metadata': self.plan_metadata
        }


class GeneratedScript(db.Model):
    """Model for storing Python Playwright scripts generated by Generator Agent"""
    
    __tablename__ = 'generated_scripts'
    
    id = db.Column(db.Integer, primary_key=True)
    created_at = db.Column(db.DateTime, nullable=False, default=datetime.utcnow, index=True)
    execution_id = db.Column(db.Integer, db.ForeignKey('execution_history.id'), nullable=True)
    plan_id = db.Column(db.Integer, db.ForeignKey('automation_plans.id'), nullable=True)
    
    script_type = db.Column(db.String(20), nullable=False, default='original')
    python_code = db.Column(db.Text, nullable=False)
    script_hash = db.Column(db.String(64), nullable=True)
    
    is_healed = db.Column(db.Boolean, nullable=False, default=False)
    healing_iterations = db.Column(db.Integer, nullable=True, default=0)
    
    def to_dict(self):
        """Convert model to dictionary"""
        return {
            'id': self.id,
            'created_at': self.created_at.isoformat() if self.created_at else None,
            'execution_id': self.execution_id,
            'plan_id': self.plan_id,
            'script_type': self.script_type,
            'python_code': self.python_code,
            'script_hash': self.script_hash,
            'is_healed': self.is_healed,
            'healing_iterations': self.healing_iterations
        }


class TraceFile(db.Model):
    """Model for storing Playwright trace files for Healer Agent"""
    
    __tablename__ = 'trace_files'
    
    id = db.Column(db.Integer, primary_key=True)
    created_at = db.Column(db.DateTime, nullable=False, default=datetime.utcnow, index=True)
    execution_id = db.Column(db.Integer, db.ForeignKey('execution_history.id'), nullable=True)
    script_id = db.Column(db.Integer, db.ForeignKey('generated_scripts.id'), nullable=True)
    
    trace_path = db.Column(db.Text, nullable=False)
    trace_analysis = db.Column(db.Text, nullable=True)
    identified_issues = db.Column(db.Text, nullable=True)
    
    def to_dict(self):
        """Convert model to dictionary"""
        return {
            'id': self.id,
            'created_at': self.created_at.isoformat() if self.created_at else None,
            'execution_id': self.execution_id,
            'script_id': self.script_id,
            'trace_path': self.trace_path,
            'trace_analysis': self.trace_analysis,
            'identified_issues': self.identified_issues
        }


class ExecutionHistory(db.Model):
    """Model for storing automation execution history"""
    
    __tablename__ = 'execution_history'
    
    id = db.Column(db.Integer, primary_key=True)
    created_at = db.Column(db.DateTime, nullable=False, default=datetime.utcnow, index=True)
    
    prompt = db.Column(db.Text, nullable=False)
    engine = db.Column(db.String(50), nullable=False)
    headless = db.Column(db.Boolean, nullable=False, default=False)
    
    success = db.Column(db.Boolean, nullable=False, default=False)
    error_message = db.Column(db.Text, nullable=True)
    
    screenshot_path = db.Column(db.Text, nullable=True)  # JSON array of screenshot paths
    execution_logs = db.Column(db.Text, nullable=True)
    
    iterations = db.Column(db.Integer, nullable=True)
    execution_time = db.Column(db.Float, nullable=True)
    
    automation_plans = db.relationship('AutomationPlan', backref='execution', lazy=True)
    generated_scripts = db.relationship('GeneratedScript', backref='execution', lazy=True)
    trace_files = db.relationship('TraceFile', backref='execution', lazy=True)
    
    def to_dict(self):
        """Convert model to dictionary"""
        screenshots = []
        if self.screenshot_path:
            try:
                screenshots = json.loads(self.screenshot_path)
            except (json.JSONDecodeError, TypeError):
                screenshots = [self.screenshot_path] if self.screenshot_path else []
        
        # Get generated and healed scripts
        generated_script = None
        healed_script = None
        
        # Find the most recent generated script (non-healed)
        for script in sorted(self.generated_scripts, key=lambda x: x.created_at, reverse=True):
            if not script.is_healed and not generated_script:
                generated_script = script.python_code
            elif script.is_healed and not healed_script:
                healed_script = script.python_code
        
        return {
            'id': self.id,
            'created_at': self.created_at.isoformat() if self.created_at else None,
            'prompt': self.prompt,
            'engine': self.engine,
            'headless': self.headless,
            'success': self.success,
            'error_message': self.error_message,
            'screenshot_path': self.screenshot_path,
            'screenshots': screenshots,
            'execution_logs': self.execution_logs,
            'iterations': self.iterations,
            'execution_time': self.execution_time,
            'generated_script': generated_script,
            'healed_script': healed_script
        }


class CredentialVault(db.Model):
    """Model for securely storing website credentials with encryption"""
    
    __tablename__ = 'credential_vault'
    
    id = db.Column(db.Integer, primary_key=True)
    created_at = db.Column(db.DateTime, nullable=False, default=datetime.utcnow, index=True)
    updated_at = db.Column(db.DateTime, nullable=False, default=datetime.utcnow, onupdate=datetime.utcnow)
    
    name = db.Column(db.String(100), nullable=False, unique=True, index=True)
    service = db.Column(db.String(200), nullable=True)
    url = db.Column(db.Text, nullable=True)
    username = db.Column(db.String(200), nullable=True)
    encrypted_value = db.Column(db.Text, nullable=False)
    description = db.Column(db.Text, nullable=True)
    
    def set_credential(self, plaintext_value: str):
        """Encrypt and store a credential value"""
        self.encrypted_value = credential_encryption.encrypt(plaintext_value)
    
    def get_credential(self) -> str:
        """Decrypt and return the credential value"""
        return credential_encryption.decrypt(self.encrypted_value)
    
    def to_dict(self, include_value=False):
        """
        Convert model to dictionary
        
        Args:
            include_value: If True, include the decrypted credential value.
                          Should only be True when actually using the credential.
        """
        result = {
            'id': self.id,
            'created_at': self.created_at.isoformat() if self.created_at else None,
            'updated_at': self.updated_at.isoformat() if self.updated_at else None,
            'name': self.name,
            'service': self.service,
            'url': self.url,
            'username': self.username,
            'description': self.description
        }
        
        if include_value:
            result['value'] = self.get_credential()
        
        return result


class SiteCrawl(db.Model):
    """Model for storing site crawl metadata and progress"""
    
    __tablename__ = 'site_crawls'
    
    id = db.Column(db.Integer, primary_key=True)
    created_at = db.Column(db.DateTime, nullable=False, default=datetime.utcnow, index=True)
    updated_at = db.Column(db.DateTime, nullable=False, default=datetime.utcnow, onupdate=datetime.utcnow)
    completed_at = db.Column(db.DateTime, nullable=True)
    
    name = db.Column(db.String(200), nullable=False)
    start_url = db.Column(db.Text, nullable=False)
    credential_id = db.Column(db.Integer, db.ForeignKey('credential_vault.id'), nullable=True)
    
    status = db.Column(db.String(50), nullable=False, default='pending', index=True)
    progress = db.Column(db.Integer, nullable=False, default=0)
    total_pages = db.Column(db.Integer, nullable=False, default=0)
    pages_crawled = db.Column(db.Integer, nullable=False, default=0)
    
    max_pages = db.Column(db.Integer, nullable=False, default=50)
    max_depth = db.Column(db.Integer, nullable=False, default=3)
    same_domain_only = db.Column(db.Boolean, nullable=False, default=True)
    
    error_message = db.Column(db.Text, nullable=True)
    crawl_logs = db.Column(db.Text, nullable=True)
    crawl_metadata = db.Column(db.Text, nullable=True)
    
    pages = db.relationship('CrawlPage', backref='crawl', lazy=True, cascade='all, delete-orphan')
    knowledge_entries = db.relationship('SiteKnowledge', backref='crawl', lazy=True, cascade='all, delete-orphan')
    credential = db.relationship('CredentialVault', backref='crawls', lazy=True)
    
    def to_dict(self, include_pages=False, include_knowledge=False):
        """Convert model to dictionary"""
        result = {
            'id': self.id,
            'created_at': self.created_at.isoformat() if self.created_at else None,
            'updated_at': self.updated_at.isoformat() if self.updated_at else None,
            'completed_at': self.completed_at.isoformat() if self.completed_at else None,
            'name': self.name,
            'start_url': self.start_url,
            'credential_id': self.credential_id,
            'status': self.status,
            'progress': self.progress,
            'total_pages': self.total_pages,
            'pages_crawled': self.pages_crawled,
            'max_pages': self.max_pages,
            'max_depth': self.max_depth,
            'same_domain_only': self.same_domain_only,
            'error_message': self.error_message,
            'metadata': json.loads(self.crawl_metadata) if self.crawl_metadata else {}
        }
        
        if include_pages:
            result['pages'] = [page.to_dict() for page in self.pages]
        
        if include_knowledge:
            result['knowledge'] = [k.to_dict() for k in self.knowledge_entries]
        
        return result


class CrawlPage(db.Model):
    """Model for storing individual pages discovered during crawl"""
    
    __tablename__ = 'crawl_pages'
    
    id = db.Column(db.Integer, primary_key=True)
    created_at = db.Column(db.DateTime, nullable=False, default=datetime.utcnow, index=True)
    crawl_id = db.Column(db.Integer, db.ForeignKey('site_crawls.id'), nullable=False, index=True)
    
    url = db.Column(db.Text, nullable=False)
    title = db.Column(db.Text, nullable=True)
    depth = db.Column(db.Integer, nullable=False, default=0)
    parent_url = db.Column(db.Text, nullable=True)
    
    screenshot_path = db.Column(db.Text, nullable=True)
    page_type = db.Column(db.String(50), nullable=True)
    
    status = db.Column(db.String(50), nullable=False, default='discovered')
    error_message = db.Column(db.Text, nullable=True)
    
    page_metadata = db.Column(db.Text, nullable=True)
    
    elements = db.relationship('PageElement', backref='page', lazy=True, cascade='all, delete-orphan')
    
    def to_dict(self, include_elements=False):
        """Convert model to dictionary"""
        result = {
            'id': self.id,
            'created_at': self.created_at.isoformat() if self.created_at else None,
            'crawl_id': self.crawl_id,
            'url': self.url,
            'title': self.title,
            'depth': self.depth,
            'parent_url': self.parent_url,
            'screenshot_path': self.screenshot_path,
            'page_type': self.page_type,
            'status': self.status,
            'error_message': self.error_message,
            'metadata': json.loads(self.page_metadata) if self.page_metadata else {}
        }
        
        if include_elements:
            result['elements'] = [elem.to_dict() for elem in self.elements]
        
        return result


class PageElement(db.Model):
    """Model for storing discovered elements on a page"""
    
    __tablename__ = 'page_elements'
    
    id = db.Column(db.Integer, primary_key=True)
    created_at = db.Column(db.DateTime, nullable=False, default=datetime.utcnow, index=True)
    page_id = db.Column(db.Integer, db.ForeignKey('crawl_pages.id'), nullable=False, index=True)
    
    element_type = db.Column(db.String(50), nullable=False)
    selector = db.Column(db.Text, nullable=False)
    xpath = db.Column(db.Text, nullable=True)
    
    text_content = db.Column(db.Text, nullable=True)
    attributes = db.Column(db.Text, nullable=True)
    
    is_interactive = db.Column(db.Boolean, nullable=False, default=False)
    is_form_field = db.Column(db.Boolean, nullable=False, default=False)
    is_link = db.Column(db.Boolean, nullable=False, default=False)
    
    position = db.Column(db.Text, nullable=True)
    description = db.Column(db.Text, nullable=True)
    
    def to_dict(self):
        """Convert model to dictionary"""
        return {
            'id': self.id,
            'created_at': self.created_at.isoformat() if self.created_at else None,
            'page_id': self.page_id,
            'element_type': self.element_type,
            'selector': self.selector,
            'xpath': self.xpath,
            'text_content': self.text_content,
            'attributes': json.loads(self.attributes) if self.attributes else {},
            'is_interactive': self.is_interactive,
            'is_form_field': self.is_form_field,
            'is_link': self.is_link,
            'position': json.loads(self.position) if self.position else {},
            'description': self.description
        }


class SiteKnowledge(db.Model):
    """Model for storing compiled knowledge about a site"""
    
    __tablename__ = 'site_knowledge'
    
    id = db.Column(db.Integer, primary_key=True)
    created_at = db.Column(db.DateTime, nullable=False, default=datetime.utcnow, index=True)
    crawl_id = db.Column(db.Integer, db.ForeignKey('site_crawls.id'), nullable=False, index=True)
    
    knowledge_type = db.Column(db.String(50), nullable=False, index=True)
    category = db.Column(db.String(100), nullable=True)
    
    title = db.Column(db.String(200), nullable=False)
    description = db.Column(db.Text, nullable=True)
    content = db.Column(db.Text, nullable=False)
    
    url = db.Column(db.Text, nullable=True)
    selector = db.Column(db.Text, nullable=True)
    
    confidence_score = db.Column(db.Float, nullable=True, default=1.0)
    knowledge_metadata = db.Column(db.Text, nullable=True)
    
    def to_dict(self):
        """Convert model to dictionary"""
        return {
            'id': self.id,
            'created_at': self.created_at.isoformat() if self.created_at else None,
            'crawl_id': self.crawl_id,
            'knowledge_type': self.knowledge_type,
            'category': self.category,
            'title': self.title,
            'description': self.description,
            'content': self.content,
            'url': self.url,
            'selector': self.selector,
            'confidence_score': self.confidence_score,
            'metadata': json.loads(self.knowledge_metadata) if self.knowledge_metadata else {}
        }
